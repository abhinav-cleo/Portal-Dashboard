dependencies:
  pre:
    - npm cache clean -f
    - npm install -g n
    - sudo n stable
    - ln -sf /usr/local/n/versions/node/<VERSION>/bin/node /usr/bin/node


deployment:
  staging:
    branch: master
    heroku:
      appname: harmonyapppoc
